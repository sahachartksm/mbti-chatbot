# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0

# LLM client (Ollama)
httpx>=0.27.0

# ML (legacy /predict — keep for backward compat)
scikit-learn>=1.5.2
numpy>=2.1.0
pandas>=2.2.3
joblib>=1.4.2

# Optional: free-text semantic matching (legacy; comment out to skip ~2GB install)
sentence-transformers>=3.3.0
torch>=2.5.0
