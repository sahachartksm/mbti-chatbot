export const environment = {
  production: false,
  // Relative URL → Angular dev-server proxy (see proxy.conf.js) forwards to backend
  // Works automatically through ngrok / any tunnel / any hostname
  apiBase: '/api',
};
