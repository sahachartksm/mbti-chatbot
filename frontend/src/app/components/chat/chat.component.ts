// DEPRECATED — replaced by QuestionnaireComponent.
// Kept as a no-op stub only to avoid dangling file references; remove freely if desired.
import { Component } from '@angular/core';

@Component({
  selector: 'app-chat-deprecated',
  standalone: true,
  template: `<!-- deprecated — use QuestionnaireComponent -->`,
})
export class ChatComponent {}
